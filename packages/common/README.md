# Common Package

Reserved for shared utilities such as config, logging, exceptions, response models, and datetime helpers.

Phase 1 keeps the API Gateway self-contained until shared package extraction is needed.
